# Mixed-version workspace fixture

One tree mixing languages and window pins (as of 2026-08):
Java 26, Python 3.12, Rust 2021, PHP 8.3, JS ES2024, Go 1.25, C11.

`cargo test` on Darwin is the stand-in for matrix CI green.
Linux CI must run the same fixtures.
